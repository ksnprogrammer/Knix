// AI Features have been disabled for this version of the platform.
// This file acts as a placeholder to prevent build errors if imports exist.

export const getGeminiClient = () => null;

export const generateTutorResponse = async () => {
  return "AI Tutor is currently disabled.";
};

export const analyzeImage = async () => {
  return "Image analysis is disabled.";
};

export const analyzeVideo = async () => {
  return "Video analysis is disabled.";
};

export const searchGrounding = async () => {
  return { text: "Search disabled.", sources: [] };
};

export const generateEducationalImage = async () => {
  return null;
};